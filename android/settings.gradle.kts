pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id("com.android.library") version "9.0.1" apply false
    }
}

rootProject.name = "xue_hua_speaker_earpiece_toggle"
